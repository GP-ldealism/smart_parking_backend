<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.gp.smartparking.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="cn.gp.smartparking.model.entity.User">
            <id property="id" column="id" />
            <result property="username" column="username" />
            <result property="password" column="password" />
            <result property="nickname" column="nickname" />
            <result property="phone" column="phone" />
            <result property="role" column="role" />
            <result property="status" column="status" />
            <result property="version" column="version" />
            <result property="createBy" column="create_by" />
            <result property="createTime" column="create_time" />
            <result property="updateBy" column="update_by" />
            <result property="updateTime" column="update_time" />
            <result property="isDeleted" column="is_deleted" />
    </resultMap>

    <sql id="Base_Column_List">
        id,username,password,nickname,phone,role,
        status,version,create_by,create_time,update_by,
        update_time,is_deleted
    </sql>

    <select id="selectByUsernameAndPassword" resultType="cn.gp.smartparking.model.vo.UserVO">
        select
            id,
            username,
            nickname,
            phone,
            `role`,
            create_time
        from
            user
        where
            username = #{username,jdbcType=VARCHAR}
          and password = #{password,jdbcType=VARCHAR}
          and status = 1
          and is_deleted = 0
    </select>

    <update id="chooseUpdateById">
        update user
        <set>
            <if test="user.nickname != null and user.nickname != ''">
                nickname = #{user.nickname, jdbcType=VARCHAR},
            </if>
            <if test="user.phone != null and user.phone != ''">
                phone = #{user.phone, jdbcType=VARCHAR},
            </if>
            <if test="user.password != null and user.password != ''">
                password = #{user.password, jdbcType=VARCHAR},
            </if>
            update_time = now()
        </set>
        where
            id = #{id,jdbcType=BIGINT}
    </update>
</mapper>
